import React from "react";

const Music = (props) => {
   
   // if(!props.isAuth) return <Navigate to='/auth'/>

   return (
      <div>
         Example Redirect
      </div>
   )
}

export default Music;